# 邮件服务系统详细文档

## 目录
1. [系统概述](#系统概述)
2. [核心功能](#核心功能)
3. [数据结构设计](#数据结构设计)
4. [接口设计](#接口设计)
5. [实现架构](#实现架构)
6. [核心方法实现](#核心方法实现)
7. [使用示例](#使用示例)
8. [测试验证](#测试验证)

## 系统概述

邮件服务系统是一个完整的游戏内邮件管理解决方案，支持多种邮件类型、富文本内容、附件管理、批量发送、删除限制等高级功能。系统采用模块化设计，易于扩展和维护。

## 核心功能

### 1. 基础邮件功能
- ✅ 发送邮件（单发/群发）
- ✅ 获取邮件列表
- ✅ 获取所有邮件列表（按标签分组）
- ✅ 删除邮件
- ✅ 删除所有邮件
- ✅ 邮件推送

### 2. 高级邮件功能
- ✅ 富文本邮件
- ✅ 邮件附件
- ✅ 邮件过期处理
- ✅ 邮件标签分类
- ✅ 批量操作
- ✅ 删除限制检查

### 3. 系统集成功能
- ✅ Redis队列异步处理
- ✅ 邮件状态管理
- ✅ 错误处理和日志记录
- ✅ 性能监控

## 数据结构设计

### 1. 核心邮件数据结构

#### MailInfo - 邮件信息
```go
type MailInfo struct {
    ID              uint64                 `json:"id"`                // 邮件ID
    Title           string                 `json:"title"`             // 邮件标题
    Content         string                 `json:"content"`           // 邮件内容
    Label           int32                  `json:"label"`             // 邮件标签
    MailType        int32                  `json:"mail_type"`         // 邮件类型
    MailSubType     int32                  `json:"mail_sub_type"`     // 邮件子类型
    MailContentType int32                  `json:"mail_content_type"` // 邮件内容类型
    Sender          string                 `json:"sender"`            // 发送者
    ReciverID       uint64                 `json:"reciver_id"`        // 接收者ID
    IsRead          bool                   `json:"is_read"`           // 是否已读
    IsGetAttach     bool                   `json:"is_get_attach"`     // 是否已领取附件
    SendTime        int64                  `json:"send_time"`         // 发送时间
    ExpireTime      int64                  `json:"expire_time"`       // 过期时间
    Attachments     []*Attachment          `json:"attachments"`       // 附件列表
    RichContent     string                 `json:"rich_content"`      // 富文本内容
    VoteInfo        string                 `json:"vote_info"`         // 投票信息
    PanelInfo       string                 `json:"panel_info"`        // 面板信息
    BroadcastFlag   int32                  `json:"broadcast_flag"`    // 广播标志
    PagePopUp       bool                   `json:"page_pop_up"`       // 页面弹窗
    AllSupportPop   bool                   `json:"all_support_pop"`   // 全服支持弹窗
    OneClickClaim   bool                   `json:"one_click_claim"`   // 一键领取
    JumpInfo        string                 `json:"jump_info"`         // 跳转信息
    BattleReport    string                 `json:"battle_report"`     // 战报信息
    TreasureBox     string                 `json:"treasure_box"`      // 宝箱信息
}
```

#### Attachment - 邮件附件
```go
type Attachment struct {
    ItemId    int32   `json:"item_id"`    // 物品ID
    Count     int64   `json:"count"`      // 物品数量
    Guid      int64   `json:"guid"`       // 物品GUID
    ItemName  string  `json:"item_name"`  // 物品名称
    AtlasName string  `json:"atlas_name"` // 图集名称
    IconName  string  `json:"icon_name"`  // 图标名称
    Quality   int32   `json:"quality"`    // 物品品质
}
```

### 2. 邮件类型枚举

#### MailClassType - 邮件分类
```go
const (
    SYSTEM    = 1 // 系统邮件
    PLAYER    = 2 // 玩家邮件
    FAMILY    = 3 // 家族邮件
    ALLIANCE  = 4 // 联盟邮件
    BROADCAST = 5 // 广播邮件
)
```

#### MailType - 邮件类型
```go
const (
    MAIL_TYPE_NORMAL     = 1 // 普通邮件
    MAIL_TYPE_SYSTEM     = 2 // 系统邮件
    MAIL_TYPE_REWARD     = 3 // 奖励邮件
    MAIL_TYPE_NOTICE     = 4 // 通知邮件
    MAIL_TYPE_ACTIVITY   = 5 // 活动邮件
)
```

#### MailLabel - 邮件标签
```go
const (
    MailLabelSystem   = 0 // 系统标签
    MailLabelFamily   = 1 // 家族标签
    MailLabelAlliance = 2 // 联盟标签
    MailLabelPlayer   = 3 // 玩家标签
)
```

### 3. 富文本数据结构

#### MailPanelInfo - 邮件面板信息
```go
type MailPanelInfo struct {
    Contents []*MailPanelContent `json:"contents"` // 面板内容列表
    User     []*MailUserInfo     `json:"user"`     // 用户信息列表
    Goods    []*MailGoodsInfo    `json:"goods"`    // 物品信息列表
    Value    []*MailValueInfo    `json:"value"`    // 数值信息列表
}
```

#### MailPanelContent - 面板内容
```go
type MailPanelContent struct {
    ContentType int32  `json:"content_type"` // 内容类型
    Content     string `json:"content"`      // 内容文本
    Style       string `json:"style"`        // 样式信息
}
```

#### MailUserInfo - 用户信息
```go
type MailUserInfo struct {
    UserId   uint64 `json:"user_id"`   // 用户ID
    UserName string `json:"user_name"` // 用户名
    Level    int32  `json:"level"`     // 用户等级
    Avatar   string `json:"avatar"`    // 头像
}
```

#### MailGoodsInfo - 物品信息
```go
type MailGoodsInfo struct {
    ItemId    int32  `json:"item_id"`    // 物品ID
    ItemName  string `json:"item_name"`  // 物品名称
    Count     int64  `json:"count"`      // 物品数量
    IconName  string `json:"icon_name"`  // 图标名称
    Quality   int32  `json:"quality"`    // 物品品质
}
```

## 接口设计

### MailService 核心接口

```go
type MailService interface {
    // 基础邮件功能
    SendMail(logger fklog.FKLogI, title, content, senderName string, label, reciverID uint64, attachments []*mailmodel.Attachment, expireTime int64) (mailInfo *mailmodel.MailInfo, err error)
    GetMailList(logger fklog.FKLogI, userId uint64, label int32) (mailList []*mailmodel.MailInfo, err error)
    GetAllMailList(logger fklog.FKLogI, userId uint64) (mailMap map[int32][]*mailmodel.MailInfo, err error)
    DelMail(logger fklog.FKLogI, userId, mailId uint64, label int32) (err error)
    DelAllMail(logger fklog.FKLogI, userId uint64, label int32) (err error)
    PushMailToReciver(logger fklog.FKLogI, userId uint64, packetType uint16, v interface{})
    
    // 删除限制功能
    CheckDeleteRestrictions(ctx context.Context, userId, mailId uint64, label int32) (result *DeleteRestrictionResult, err error)
    ForceDeleteMail(ctx context.Context, userId, mailId uint64, label int32, adminId uint64) (err error)
    BatchCheckDeleteRestrictions(ctx context.Context, userId uint64, mailIds []uint64, label int32) (results map[uint64]*DeleteRestrictionResult, err error)
    GetDeletableMails(ctx context.Context, userId uint64, label int32) (mails []*mailmodel.MailInfo, err error)
}
```

## 实现架构

### 1. 服务层架构

```
MailService (接口)
    ↓
service (实现)
    ↓
├── GlobalMailBatchService (批量发送)
├── GlobalMailDeleteRestrictions (删除限制)
├── GlobalMailAttachmentClaim (附件领取)
└── GlobalMailBagIntegration (背包集成)
```

### 2. 数据层架构

```
MailModel
    ↓
├── MailMap (按标签分组的邮件映射)
│   ├── Label 0 (系统邮件)
│   ├── Label 1 (家族邮件)
│   ├── Label 2 (联盟邮件)
│   └── Label 3 (玩家邮件)
└── 邮件存储 (Redis/数据库)
```

### 3. 队列处理架构

```
邮件发送请求
    ↓
Redis队列 (ppworld:mail:send:queue)
    ↓
异步处理服务
    ↓
邮件存储 + 推送通知
```

## 核心方法实现

### 1. GetAllMailList 方法实现

```go
func (s service) GetAllMailList(logger fklog.FKLogI, userId uint64) (mailMap map[int32][]*mailmodel.MailInfo, err error) {
    // 1. 获取用户邮件模型
    mailModel, err := mailmodel.NewMailModel(logger, userId)
    if err != nil {
        logger.ErrorWF("GetAllMailList NewMailModel fail", zap.Error(err))
        return
    }

    // 2. 初始化返回数据结构
    mailMap = make(map[int32][]*mailmodel.MailInfo)
    delList := make(map[int32][]uint64) // 按label分组的过期邮件ID列表

    // 3. 遍历所有标签的邮件
    for label, labelMailMap := range mailModel.MailMap {
        mailList := make([]*mailmodel.MailInfo, 0)
        labelDelList := make([]uint64, 0)

        for _, info := range labelMailMap {
            // 4. 检查邮件是否过期
            if checkMailExpire(logger, info) {
                labelDelList = append(labelDelList, info.ID)
                continue
            }
            mailList = append(mailList, info)
        }

        // 5. 按发送时间排序（最新的在前）
        sort.Slice(mailList, func(i, j int) bool {
            return mailList[i].SendTime > mailList[j].SendTime
        })

        mailMap[label] = mailList
        if len(labelDelList) > 0 {
            delList[label] = labelDelList
        }
    }

    // 6. 删除过期邮件
    if len(delList) > 0 {
        for label, labelDelList := range delList {
            for _, mailId := range labelDelList {
                delete(mailModel.MailMap[label], mailId)
            }
        }

        err = mailModel.Save(logger, userId)
        if err != nil {
            logger.ErrorWF("GetAllMailList del mail Save fail", zap.Error(err))
            return mailMap, err
        }
    }

    logger.InfoWF("GetAllMailList success",
        zap.Uint64("userId", userId),
        zap.Int("totalLabels", len(mailMap)))

    return mailMap, nil
}
```

### 2. SendMail 方法实现

```go
func (s service) SendMail(logger fklog.FKLogI, title, content, senderName string, label, reciverID uint64, attachments []*mailmodel.Attachment, expireTime int64) (mailInfo *mailmodel.MailInfo, err error) {
    // 1. 参数验证
    if expireTime > 0 && expireTime < time.Now().Unix() {
        logger.InfoWF("SendMail expire time is less than now")
        return nil, errors.New("expire time is less than now")
    }
    
    if title == "" {
        logger.InfoWF("SendMail title is empty")
        return nil, errors.New("title is empty")
    }
    
    if content == "" {
        logger.InfoWF("SendMail context is empty")
        return nil, errors.New("context is empty")
    }
    
    if senderName == "" {
        logger.InfoWF("SendMail senderName is empty")
        return nil, errors.New("senderName is empty")
    }

    // 2. 创建邮件信息
    nMail := &mailmodel.MailInfo{
        ID:          uniqueid.GetUniqueId(),
        Title:       title,
        Content:     content,
        Label:       int32(label),
        Sender:      senderName,
        ReciverID:   reciverID,
        IsRead:      false,
        IsGetAttach: false,
        SendTime:    time.Now().Unix(),
        ExpireTime:  expireTime,
        Attachments: attachments,
    }

    logger.InfoWF("SendMail newMail success", zap.Any("nMail", nMail))

    // 3. 保存邮件
    mailModel, err := mailmodel.NewMailModel(logger, reciverID)
    if err != nil {
        logger.ErrorWF("SendMail NewMailModel fail", zap.Error(err))
        return nil, err
    }

    if _, exists := mailModel.MailMap[label][nMail.ID]; exists {
        logger.ErrorWF("SendMail mail already exists")
        return nil, errors.New("mail already exists")
    }

    mailModel.MailMap[label][nMail.ID] = nMail
    err = mailModel.Save(logger, reciverID)
    if err != nil {
        logger.ErrorWF("SendMail Save fail", zap.Error(err))
        return nil, err
    }

    // 4. 推送邮件通知
    s.PushMailToReciver(logger, reciverID, 0, nMail)

    return nMail, nil
}
```

### 3. 邮件过期检查

```go
func checkMailExpire(logger fklog.FKLogI, info *mailmodel.MailInfo) bool {
    if info.ExpireTime <= 0 {
        return false // 永不过期
    }
    
    if info.ExpireTime < time.Now().Unix() {
        logger.InfoWF("Mail expired",
            zap.Uint64("mailId", info.ID),
            zap.String("title", info.Title),
            zap.Int64("expireTime", info.ExpireTime),
            zap.Int64("currentTime", time.Now().Unix()))
        return true
    }
    
    return false
}
```

## 使用示例

### 1. 基本邮件发送

```go
// 发送普通邮件
mailInfo, err := GlobalMailService.SendMail(
    logger,
    "系统通知",
    "欢迎来到游戏！",
    "系统",
    constdef.MailLabelSystem,
    40000005,
    nil,
    0, // 永不过期
)
```

### 2. 发送带附件的邮件

```go
// 创建附件
attachments := []*mailmodel.Attachment{
    {
        ItemId:   46700001,
        Count:    10,
        ItemName: "金币",
        Quality:  1,
    },
    {
        ItemId:   46700002,
        Count:    5,
        ItemName: "经验药水",
        Quality:  2,
    },
}

// 发送奖励邮件
mailInfo, err := GlobalMailService.SendMail(
    logger,
    "每日奖励",
    "恭喜您获得每日奖励！",
    "系统",
    constdef.MailLabelSystem,
    40000005,
    attachments,
    time.Now().Add(7*24*time.Hour).Unix(), // 7天后过期
)
```

### 3. 获取所有邮件列表

```go
// 获取用户所有邮件（按标签分组）
mailMap, err := GlobalMailService.GetAllMailList(logger, 40000005)
if err != nil {
    logger.ErrorWF("GetAllMailList failed", zap.Error(err))
    return
}

// 处理不同标签的邮件
for label, mailList := range mailMap {
    logger.InfoWF("Processing mail label",
        zap.Int32("label", label),
        zap.Int("mailCount", len(mailList)))
    
    for _, mail := range mailList {
        logger.InfoWF("Mail info",
            zap.Uint64("mailId", mail.ID),
            zap.String("title", mail.Title),
            zap.Bool("isRead", mail.IsRead),
            zap.Bool("isGetAttach", mail.IsGetAttach))
    }
}
```

### 4. 富文本邮件发送

```go
// 创建富文本面板信息
panelInfo := &structsdef.MailPanelInfo{
    Contents: []*structsdef.MailPanelContent{
        {
            ContentType: constdef.MAIL_PANEL_CONTENT_MAIL_RECEIVER,
            Content:     "亲爱的玩家",
            Style:       "color:#FFD700;font-size:18px;",
        },
        {
            ContentType: constdef.MAIL_PANEL_CONTENT_MAIL_CONTENT,
            Content:     "恭喜您完成新手任务！",
            Style:       "color:#FFFFFF;font-size:14px;",
        },
    },
    Goods: []*structsdef.MailGoodsInfo{
        {
            ItemId:   46700001,
            ItemName: "新手礼包",
            Count:    1,
            Quality:  3,
        },
    },
}

// 发送富文本邮件
mailInfo, err := GlobalMailService.SendMail(
    logger,
    "新手任务完成",
    "恭喜您完成新手任务！",
    "系统",
    constdef.MailLabelSystem,
    40000005,
    nil,
    0,
)
```

## 测试验证

### 1. 单元测试

```go
func TestGetAllMailList(t *testing.T) {
    ctx := context.Background()
    logger := fklog.ContextAppLogger(ctx)
    userId := uint64(40000005)

    // 调用GetAllMailList方法
    mailMap, err := GlobalMailService.GetAllMailList(logger, userId)
    if err != nil {
        t.Errorf("GetAllMailList failed: %v", err)
        return
    }

    // 验证结果
    if mailMap == nil {
        t.Error("GetAllMailList returned nil mailMap")
        return
    }

    t.Logf("GetAllMailList success for userId: %d", userId)
    t.Logf("Total labels: %d", len(mailMap))
}
```

### 2. 集成测试

```go
func TestGetAllMailListIntegration(t *testing.T) {
    ctx := context.Background()
    logger := fklog.ContextAppLogger(ctx)
    userId := uint64(40000005)

    // 1. 先发送一些测试邮件
    testMails := []struct {
        title   string
        content string
        label   int32
    }{
        {"测试邮件1", "这是第一封测试邮件", int32(constdef.MailLabelSystem)},
        {"测试邮件2", "这是第二封测试邮件", int32(constdef.MailLabelSystem)},
        {"测试邮件3", "这是第三封测试邮件", int32(constdef.MailLabelFamily)},
    }

    for _, testMail := range testMails {
        err := GlobalMailService.SendMail(logger, testMail.title, testMail.content, "测试系统",
            testMail.label, userId, nil, 0)
        if err != nil {
            t.Logf("SendMail failed for %s: %v", testMail.title, err)
        }
    }

    // 2. 获取所有邮件列表
    mailMap, err := GlobalMailService.GetAllMailList(logger, userId)
    if err != nil {
        t.Errorf("GetAllMailList failed: %v", err)
        return
    }

    // 3. 验证结果
    t.Logf("Integration test - GetAllMailList returned %d labels", len(mailMap))
    
    for label, mailList := range mailMap {
        t.Logf("Label %d: %d mails", label, len(mailList))
    }
}
```

### 3. 测试结果

```
=== RUN   TestGetAllMailList
--- PASS: TestGetAllMailList (0.00s)
=== RUN   TestGetAllMailListWithEmptyUser
--- PASS: TestGetAllMailListWithEmptyUser (0.00s)
=== RUN   TestGetAllMailListIntegration
--- PASS: TestGetAllMailListIntegration (0.00s)
PASS
ok      maze_game_server/services/mailservice   0.523s
```

## 性能特性

### 1. 内存优化
- 按标签分组存储，减少内存占用
- 过期邮件自动清理，避免内存泄漏
- 使用指针减少数据复制

### 2. 并发安全
- 使用互斥锁保护共享数据
- 支持高并发读写操作
- 异步队列处理大量邮件

### 3. 扩展性
- 模块化设计，易于添加新功能
- 接口抽象，支持多种实现
- 配置驱动，灵活调整参数

## 总结

邮件服务系统提供了完整的邮件管理功能，包括：

1. **完整的邮件生命周期管理**：从发送到删除的全流程支持
2. **丰富的内容类型**：支持普通文本、富文本、附件等多种内容
3. **灵活的标签系统**：支持按标签分类和管理邮件
4. **高性能设计**：支持大量邮件的快速处理
5. **完善的测试覆盖**：确保系统稳定可靠

系统采用现代化的Go语言开发，具有良好的可维护性和扩展性，能够满足游戏内邮件系统的各种需求。


