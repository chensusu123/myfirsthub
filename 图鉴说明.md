图鉴系统架构梳理
1. 核心数据结构
图鉴信息结构 (NPC_LIBRARY_INFO)
struct NPC_LIBRARY_INFO
{
    uint64 muint64Id;           // 数据库索引
    uint64 mHostUserId;         // 主人用户ID
    uint64 mNPCRoleId;          // NPC角色ID
    uint64 mNPCUserID;          // NPC用户ID
    int32  mint32NPCCount;      // NPC个数
    int32  mint32NPCLevel;      // 图鉴级别
    uint64 muint64CreateTime;   // 创建时间(初次解锁时间)
};
扩展图鉴信息 (NPC_LIBRARY_INFO_EX)
struct NPC_LIBRARY_INFO_EX : NPC_LIBRARY_INFO
{
    uint64 muint64NextLevelNeedCount;  // 下一级需要数量
    int32  mint32NextLevelCost;       // 下一级消耗
    int32  mint32NextLevelCostType;   // 消耗类型
    int32  miComposeFlag;              // 合成标记
    int32  miNextCostStrength;         // 下一级消耗强度
    int32  mnextRequiredLevel;        // 下一级要求等级
    int32  mihp;                      // 血量
    int32  miFightVal;                // 战斗力
    int32  miPrestige;                // 声望
    int32  miComposeNeedCnt;          // 合成需要数量
    int32  miOrder;                   // 排序
    int32  miQuality;                 // 品质
    int32  miGroupOrder;              // 组排序
};
2. 协议定义
图鉴相关RPC协议类型：
DEF_SVR_NPC_LIBRARY_ADD_RQ/RS - 添加图鉴
DEF_SVR_NPC_LIBRARY_QUERY_RQ/RS - 查询图鉴
DEF_SVR_NPC_LIBRARY_NOTIFY - 变化通知
DEF_SVR_NPC_LIBRARY_UPGRADE_RQ/RS - 图鉴升级
DEF_SVR_NPC_LIBRARY_CHECK_RQ/RS - 图鉴检查
DEF_SVR_NPC_LIBRARY_NEXTLEVEL_INFO_RQ/RS - 下一级图鉴信息
DEF_SVR_NPC_LIBRARYS_CHECK_RQ/RS - 批量图鉴检查
3. 业务逻辑实现
图鉴来源类型 (LibFromType)：
enum LibFromType
{
        LibFromType_cli_refresh = 1; // 战斗抓取
        LibFromType_npcvalet_drop = 2; // 跟班打工掉落
        LibFromType_treaurebox_drop = 3; // 普通抽卡掉落
        LibFromType_loot = 4; // 顺
        LibFromType_free_treaurebox_drop = 5; // 免费开宝箱掉落
        LibFromType_new_user_guide_award = 6; // 新手引导掉落
        LibFromType_vip_treaurebox_drop = 7; // vip抽卡
        LibFromType_client_buy_npc = 8; // 客户端购买
        LibFromType_vip_receive_awards = 100; // vip领取奖励
        LibFromType_new_register_awards = 9; // 新注册赠送星卡
        LibFromType_mini_apps_new_register = 10; //h5版小程序用户到客户端登录注册迁移星卡
        LibFromType_mall_buy = 11; //商城购买
        LibFromType_php_active = 12; //PHP活动 吴辉
        LibFromType_loot_slot = 13; //领取槽位奖励
        LibFromType_rob_slot = 14; //打劫槽位奖励
        LibFromType_explore_award = 15; //游戏探索奖励
        LibFromType_exchange_npc = 270; //提现页兑换星卡
        LibFromType_background = 2000; //后台操作
        LibFromType_up_progress = 2001; //使用碎片
        LibFromType_up_star = 2002; //升星
        LibFromType_full_star_clean = 2003; //满星后清除碎片
        LibFromType_day_task = 3000;//日常任务
    LibFromType_section_task = 3001;//章节任务
    LibFromType_achievement = 3002;//成就
        LibFromType_resource_battle = 3003;//资源站奖励星卡碎片
        LibFromType_diamond_shop = 3004;//钻石商店发送
        LibFromType_web_shop = 3005;//web商店-礼包
        LibFromType_redpacket_shop = 3006;//红包商城
        LibFromType_send_sys_user_id = 3007;//后台造账号  比方说造主持账号给第一个种子
        LibFromType_peiyu_ai = 3008;//培育ai账号
        LibFromType_crit_add_value = 9000; //暴击升星时,暴击出的额外碎片
}
4. 数据库操作接口
核心数据库操作：
AddUserNpcLib() - 添加用户图鉴
UpNpcLib() - 更新图鉴信息
SetNpcLib() - 设置图鉴信息
GetNpcLib() - 获取图鉴信息
存储过程调用：
// 添加图鉴存储过程
FTalk_a_snprintf(lszTmp, 200, "pr_add_user_npc_lib_%llu", (auint64HostUserId >> 8) % 64);
FTalk_a_snprintf(lszSQL, DEF_SQLSTRING_SIZE, "call %s(%llu, %llu, %llu, %u, %u, %u)",
    lszTmp, auint64HostUserId, aoNpcLibInfo.mNPCRoleId, aoNpcLibInfo.mNPCUserID, 
    aintIncreseNpcLibCnt, aoNpcLibInfo.mint32NPCLevel, aintFromType);
5.RPC客户端接口
UserNpcRpcClient 核心方法：
addNpcCard() - 添加NPC卡片
getNPCLibraryCheck() - 检查图鉴是否存在
getNPCLibraryInfo() - 获取图鉴列表
getNPCLibraryNextLevelInfo() - 获取下一级图鉴信息
forceComposeNPC() - 强制合成NPC
6.系统流程
图鉴系统主要流程：
1. 图鉴获取流程：
- 通过不同来源获得NPC卡片/碎片
- 调用 addNpcCard() 添加到用户图鉴
- 数据库存储图鉴信息
- 通知客户端图鉴变化
2. 图鉴升级流程：
- 检查升级条件（数量、等级要求等）
- 扣除升级消耗
- 更新图鉴等级和属性
- 返回升级结果
3. 图鉴查询流程：
- 支持分页查询
- 使用游标和变化令牌
- 返回图鉴列表和状态信息
4. 图鉴检查流程：
- 单个或批量检查图鉴存在性
- 返回检查结果
7.关键特性
- 分片存储： 使用用户ID进行分片存储
- 增量更新： 支持增量添加图鉴数量
- 版本控制： 使用变化令牌进行版本控制
- 多来源支持： 支持多种图鉴获取来源
- 升级系统： 支持图鉴等级提升和属性增强
- 批量操作： 支持批量检查和操作
总结
图鉴系统是一个完整的NPC收集和管理系统，主要特点包括：
1. 数据层： 使用分片存储，支持大量用户数据
2. 协议层： 完整的RPC协议定义，支持增删改查操作
3. 业务层： 丰富的图鉴来源和升级机制
4. 接口层： 统一的RPC客户端接口，便于其他服务调用
该系统支持NPC卡片的收集、升级、查询等完整功能，是游戏中的重要收集系统。通过多种来源获取NPC卡片，玩家可以建立自己的图鉴收藏，并通过升级提升NPC的属性和价值。


